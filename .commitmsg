refactor(v3.0): unified interface, diagnostic reports, reduced logging

Architecture:
- Remove old dialog system (11 files, ~5500 lines deleted)
- Add dialog_v2.py: streamlined single-window interface with QgsCollapsibleGroupBox
- Add batch_orchestrator.py: bridges dialog_v2 with workflow layer
- Add batch_runner.py: sequential module execution engine
- Add project_detector.py: auto-detect project structure (MAJ/CAP FT/COMAC/GraceTHD)
- Consolidate MAJ BD to single SQL background path (Maj_Ft_Bt.py -716 lines)
- Simplify maj_workflow.py to single entry point (-168 lines)
- PoleAerien.py reduced to 128-line entry point (was 2136)

Features:
- Add unified_report.py: diagnostic Excel with per-module sub-verifications
- Each module shows individual check percentages (not just aggregate)
- COMAC cable verification pipeline (fddcpi2 cache, comparer_comac_cables)
- cable_analyzer.py: shared capacites_compatibles() extracted from PoliceC6

Performance:
- Reduce verbose logging: remove per-file COMAC, per-study Police C6 logs
- Remove debug print() from Comac.py LectureFichiersPCM
- Remove redundant PostgreSQL connection logs from db_connection.py
- Cache fddcpi2 results across COMAC and Police C6 in batch runs

Docs:
- Full-viewport hero with poleaerien.jpeg cover image as background
- Transparent navbar with scroll-triggered solid transition
- Architecture reference table section with module/infrastructure details
